spring:
  datasource:
    url: jdbc:postgresql://evaluation-db:5432/evaluation_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
